<resources>

    <style name="AppTheme" parent="@style/_AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/color_primary_dark</item>
    </style>

    <style name="AppTheme.Toolbar">
        <item name="android:background">@color/primary_text</item>
        <item name="android:backgroundTint">@color/button_bg_color</item>
        <item name="android:textColor">@color/color_white</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:titleTextStyle">@style/AppTheme.Toolbar.TitleTextStyle</item>
        <item name="android:colorPrimaryDark">@color/color_primary_dark</item>
        <item name="android:colorPrimary">@color/color_primary</item>
    </style>

    <style name="AppTheme.Toolbar.TitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/color_white</item>
    </style>

    <style name="_AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimary">@color/color_primary</item>
        <item name="android:colorPrimaryDark">@color/color_primary_dark</item>
        <item name="android:colorAccent">@color/color_primary_accent</item>

        <item name="android:backgroundTint">@color/app_bg_color</item>
        <item name="android:divider">#BDBDBD</item>
    </style>

</resources>
